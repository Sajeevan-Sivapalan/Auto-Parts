<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Automate" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!--    <style name="Theme.Automate" parent="Base.Theme.Automate" />-->
    <style name="Theme.Automate" parent="Base.Theme.Automate" />

    <style name="Theme.Globe_Carry" parent="Base.Theme.Automate" />
    <style name="ThemeOverlay.MyPopupTheme" parent="Base.Theme.Automate">
        <item name="android:colorBackground">@color/primary</item>
    </style>
</resources>